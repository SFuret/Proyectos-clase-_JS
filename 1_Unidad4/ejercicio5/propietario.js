export class Propietario {  // Exporto la clase Propietario

  
  // Las variables definidas dentro del constructor son variables de instancia
  constructor(nombre, planta, puerta) {
    this.nombre = nombre;
    this.planta = planta;
    this.puerta = puerta;
  }
}